<% @users.each do |user| %>
<ul>
  <% if user.available == true && user != current_user %>
  <li><%= link_to "#{user.username}", user_display_path(user) %></li>
  <h4>I'm available!</h4>
  <% end %>
</ul>
<% end %>