<% @users.each do |user| %>
<ul class="list-group">
  <% if user.available == true && user != current_user %>
  <li class="list-group-item"><h4>
    <%= link_to "#{user.username}", user_display_path(user) %></h4>
  </li>
  <% end %>
</ul>
<% end %>