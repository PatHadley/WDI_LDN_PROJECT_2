<nav class="container-fluid">
  <ul class="nav nav-tabs">
    <li class="<%= cp(root_path)%>" role="presentation"><%= link_to "Home", root_path %></li>
    <% if current_user %>
      <li class="<%= cp(user_list_path)%>" role="presentation"><%= link_to "Find GreenDiggers", user_list_path, class: cp(user_list_path) %></li>
      <li class="<%= cp(my_profile_path)%>" role="presentation"><%= link_to "#{current_user.username}", my_profile_path %></li>
      <li class="<%= cp(destroy_user_session_path)%>" role="presentation"><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
    <% else %>
      <li class="<%= cp(new_user_session_path)%>"role="presentation"><%= link_to "Sign in", new_user_session_path %></li>
      <li class="<%= cp(new_user_registration_path)%>" role="presentation"><%= link_to "Register", new_user_registration_path %></li>
    <% end %>
  </ul>
</nav>